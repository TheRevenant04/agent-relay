apiVersion: v1
kind: Service
metadata:
  name: relay
  namespace: agent-relay
  labels:
    app: relay
    component: api
spec:
  type: NodePort
  selector:
    app: relay
    component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080