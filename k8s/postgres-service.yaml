apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agent-relay
  labels:
    app: relay
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: relay
    component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres